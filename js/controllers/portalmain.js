angular.module('devPortal')
	.controller('PortalMainController',['$http', '$scope', '$routeParams', function($http, $scope, $routeParams){
    $scope.viewLoaded = false;

		//Functions Available to our View
		$scope.createNote = function(noteInfo){


	};
	}]);
